import { Link } from "react-router-dom";
 
const Logo = () => {
    return(
        <Link to="/" className="text-red-600 font-bold text-2xl
         lg:text-3xl">Ecommerce</Link>
    );
};

export default Logo;